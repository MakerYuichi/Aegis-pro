## Summary

<!-- What changed and why. -->

## Test plan

- [ ] `cd backend/orchestrator && LLM_PROVIDER=mock LLM_FALLBACKS= pytest tests/ -v`
- [ ] UI change: exercised `/demo` or the affected dashboard route
- [ ] No secrets in the diff
